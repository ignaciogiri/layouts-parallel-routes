{
  "pages": {
    "home": {
      "title": "Home",
      "description": "Welcome to the parallel routes demo. Navigate using the sidebar to see how header slots behave."
    },
    "about": {
      "title": "About",
      "description": "This page demonstrates a different header configuration with a back button and share action."
    },
    "editor": {
      "title": "Editor",
      "description": "The editor page shows a minimal header with just the logo centered."
    },
    "settings": {
      "title": "Settings",
      "description": "Settings page uses the same header as Home. Watch if the SearchBox remounts when navigating here."
    },
    "profile": {
      "title": "Profile",
      "description": "Profile page also uses the same header as Home. The SearchBox should ideally stay mounted."
    }
  },
  "common": {
    "signIn": "Sign in",
    "share": "Share",
    "about": "About"
  },
  "search": {
    "suggestions": {
      "items": "Search for items",
      "vintage": "Find vintage clothing",
      "electronics": "Discover electronics",
      "furniture": "Look for furniture"
    }
  },
  "header": {
    "aboutPage": "About page",
    "customCenterSlot": "Custom center slot"
  },
  "demo": {
    "title": "Parallel Routes Demo",
    "current": "Current",
    "route": "Route",
    "expectedHeader": "Expected header",
    "mountIndicator": "The purple timer shows how long each slot has been mounted.",
    "problem": {
      "title": "The Problem",
      "description": "Navigate between routes and watch the header slots. The timer resets = component remounted."
    },
    "withoutCache": {
      "title": "Without cacheComponents",
      "description": "All parallel route slots remount on every navigation. Even <code>/settings</code> and <code>/profile</code> (same SearchBox component) cause remounts."
    },
    "withCache": {
      "title": "With cacheComponents: true",
      "description": "Components don't remount, BUT each route has isolated state. <code>/settings</code> and <code>/profile</code> have separate SearchBox instances - animation resets when switching between them."
    },
    "homeException": {
      "title": "The <code>/</code> exception",
      "description": "Uses <code>default.tsx</code> (no dynamic segment) - doesn't trigger remount behavior."
    },
    "expected": {
      "title": "Expected Behavior",
      "description": "When <code>/settings</code> and <code>/profile</code> both render SearchBox, React should keep it mounted (same component type). Only remount when switching to different content (e.g., <code>/about</code>)."
    }
  }
}
